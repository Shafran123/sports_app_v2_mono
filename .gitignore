# dependencies
node_modules/
.pnpm-store/

# next.js
.next/
out/
build/
*.tsbuildinfo

# turbo
.turbo/

# env
.env
.env*.local

# tests
coverage/

# misc
.DS_Store
*.log

# firebase service account (never commit)
*.json
sports-app-20029-firebase*.json
# sp_be manifests are required by Railway's nixpacks build
!/sp_be/package.json
!/sp_be/package-lock.json
# Vercel deployment configs (in each app root)
!/apps/user/vercel.json
!/apps/admin/vercel.json
!/apps/landing/vercel.json
# each app's package.json + tsconfig are required by Vercel's framework detection + path alias resolution
!/apps/user/package.json
!/apps/admin/package.json
!/apps/landing/package.json
!/apps/user/tsconfig.json
!/apps/admin/tsconfig.json
!/apps/landing/tsconfig.json

# backend local uploads (persist on the Railway volume, not in git)
/sp_be/uploads/
.vercel
