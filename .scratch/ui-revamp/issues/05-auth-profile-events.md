# 05 — Auth, profile, events, notifications

**What to build:** the remaining player surfaces in the new identity: login/register/admin-login (new styles + no more indigo), events list/detail/register, notifications list, and profile/settings.

**Blocked by:** 01 — Design system; 02 — App shells.

**Status:** ready-for-agent

- [ ] Login/register/admin-login restyled (dark cards, pill CTAs, Google button, teleport OTP states); empty/error states
- [ ] `/events` + `/events/[id]` restyled per contracts (cards, capacity progress, pricing, cancel button)
- [ ] `/notifications` list restyled (unread lemon-left-border, read-all)
- [ ] `/profile` restyled; `npm run build` green