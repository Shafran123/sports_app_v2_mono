# 07 — Leads UI

**What to build:** the lead form is visible and the admin queue is usable. The user-app home gains a "Want to list your place?" banner CTA and a `/become-owner` page with the form; submitting shows a confirmation. The admin sidebar gains a "Leads" tab listing leads with status, the duplicate flag, and actions (contact, convert, close). Converting a lead pre-fills the create-owner form (ticket 08).

**Blocked by:** 06 — Leads backend

**Status:** ready-for-agent

- [ ] Home banner CTA links to the /become-owner form
- [ ] Public form submits and shows a confirmation
- [ ] Admin "Leads" tab lists leads with status and duplicate flags
- [ ] Admin can mark contacted / convert / close from the list
- [ ] Convert pre-fills the create-owner form